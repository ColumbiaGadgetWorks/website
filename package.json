{
  "name": "cgw-website",
  "private": true,
  "scripts": {
    "build": "hugo --minify --gc",
    "dev": "hugo server",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "hugo-extended": "0.165.0",
    "wrangler": "^4.85.0"
  }
}
